apiVersion: v1
kind: Service
metadata: # kpt-merge: /sink-service
  name: sink-service
  labels:
    app: sink-host-netdevice
    release: firewall-host-netdevice
    chart: sink-host-netdevice
  annotations:
    internal.kpt.dev/upstream-identifier: '|Service|default|sink-service'
spec:
  type: NodePort
  ports:
  - port: 667
    nodePort: 30667
  selector:
    app: sink-host-netdevice
    release: firewall-host-netdevice
